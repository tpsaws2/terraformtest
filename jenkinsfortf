pipeline {
    agent any 
      stages {
        stage('Test') { 
            steps {
                echo 'running the script now'
                sh 'terraform init'
            }
        }
      }
 }
